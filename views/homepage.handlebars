<!-- Loop over each object in the bookss array -->
{{#each posts as |post| }}
<div class="row mb-4 book">
    <div class="col-md-5">
        <h2>
            <a href="/post/{{post.id}}">{{post.name}}</a>
        </h2>
       <span for="img" aria-label="book">📖</span>
        </p>
        <p>Created by {{user.username}} </p>
    </div>
    <div class="col-md-7">
        <h5>Content:</h5> 
        <p>   
           {{post.content}}
        </p>
    </div>
</div>
{{/each}}